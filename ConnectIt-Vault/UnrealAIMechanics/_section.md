---
type: code-unit
lead: high-level.md
schemas: [updates, code]
---

# UnrealAIMechanics — section

Weighted-scoring "Utility AI" action-picker plugin (currently dormant). Lead doc: [[UnrealAIMechanics/high-level|high-level.md]].

Governed subfolders:

- `updates/` → [[_schema/updates|_schema/updates.md]] (created on first use)
- `code/` → [[_schema/code|_schema/code.md]] — see [[UnrealAIMechanics/code/index|code/index.md]]

No `systems/` or `recipes/` while the plugin is a stub (`SelectEvaluatedAction`
unimplemented, not enabled in `ConnectIt.uproject`). Add them to `schemas` when it is
revived.
