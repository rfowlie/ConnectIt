---
source: claude-code
run: 2026-09-10-1100
scope: vault
tags: [log, migration, code, re-ingest]
---

## Request

Item 5 of the `_core/` migration — the large one: re-ingest every domain's `code/` to the
new `_code` schema (nested `systems/` + `recipes/`, per-page `## Changes`, `index.md` =
inventory + Map, `commit:` re-anchored to last-commit-touching-source). User's "Full
re-ingest" choice. This note covers the tail of that work done in this session; the bulk
of the plugin re-ingest happened in the 2026-09-10-0900 session's follow-on.

## Overview

- **em-dash mojibake repaired across 34 `code/` type pages.** A prior pass's `perl`
  one-liners ran without `-C`, re-encoding every `—` / `→` / `…` / `≥` / `↔` / `⇒` / `≠`
  as `Ã¢ÂÂ`-style mojibake (byte sequence `c3 a2 c2 8x c2 8x`). Fixed with a byte-level
  `s/\xc3\xa2\xc2([\x80-\xbf])\xc2([\x80-\xbf])/\xe2$1$2/g` — the universal reverse of
  "UTF-8 read as Latin-1, re-encoded as UTF-8". Correct em-dashes added by the earlier
  `## Changes` inserts were left untouched (they were already valid UTF-8). Verified: zero
  `c3 a2 c2` sequences remain.
- **`## See also` sections normalised on ~45 `code/` pages.** The same prior pass stripped
  `[[old/…]]` links but left the wreckage — `X; X` duplicates, orphaned `;  → *Section*`
  fragments, `- In-repo:  → text.` lines with nothing before the arrow, `]], .` tails.
  Each damaged `- In-repo:` bullet was rebuilt to `- In-repo: [[<domain>/CLAUDE|<domain>
  overview]] [→ *Section*].`; bullets with no surviving link target were dropped.
- **ConnectIt `code/` pages brought current** — see below and the 2026-09-10-1200 note.
- Plugin `code/index.md` files, per-type `## Changes`, and folder nesting
  (`systems/` → `code/systems/`, `recipes/` → `code/recipes/`) were already done in the
  0900 session; provenance `commit:` anchors verified against
  `git log -1 --format=%h -- <source>`.

## Created

- `ConnectIt/code/UConnectIt_BoardRegistrySubsystem.md` — covers `UConnectIt_BoardRegistrySubsystem`
  + `UConnectIt_TileRegistry` / `UConnectIt_PieceRegistry` (the 2026-09-09 registry move,
  previously only in `ConnectIt/updates/`).
- `ConnectIt/code/runtime-state.md` — the by-system accessor reference, from the in-repo
  `RuntimeStateAccess.md`.

## Changed

- `ConnectIt/code/index.md` — full rewrite: inventory (+ `UConnectIt_BoardRegistrySubsystem`,
  legacy rows marked), `## Map` (folded suite architecture), `## Known issues` (from the
  in-repo `Duplication.md` + `Conventions.md` + the 2 confirmed bugs).
- `ConnectIt/code/UConnectIt_State_Game.md`, `UConnectIt_GameFacade.md`,
  `systems/game-state-machine.md` — **reframed as retired legacy / dead code** (were
  documented as a live "local phase model" / "read/write split"). `role:` → `internal`,
  `systems/index.md` row → `legacy`.
- `ConnectIt/code/AConnectIt_GameMode.md` — `## Changes` gained the registry-move and
  board-overhaul entries folded from `ConnectIt/updates/`; See-also repointed to the new
  decision notes.
- `UnrealGameMechanics/code/UGameEventTaskManager.md` — Gotchas gained the `Create()` vs
  `NewObject(owner)` GC-ownership note (from the in-repo `Systems.md`).
- `UnrealGridMechanics/code/systems/hover-relay.md` — See-also repopulated (was emptied by
  the normalisation).
- ~45 `code/**/*.md` — mojibake + See-also normalisation (list in the run output).

## Deleted

- `ConnectIt/updates/` (`raw/` + `wiki/` + folder) — the one note it held
  (`2026-09-09-board-registry-subsystem`) is folded into `## Changes` on
  `AConnectIt_GameMode.md` and `UConnectIt_BoardRegistrySubsystem.md`.

## Flags & Follow-ups

- ConnectIt `code/` still has ~15 `stub` rows in the inventory (controllers, libraries,
  MinMax, UI, influence map) — listed, not yet written. Not in scope for the migration;
  they were `stub` before it too.
- The legacy MVVM pipeline is documented-as-dead but still in the source tree; deleting it
  is a separate C++ change (see `ConnectIt/decisions/2026-09-08-retire-legacy-mvvm-pipeline`).
- No source files under `Plugins/` or `Source/` were touched.
