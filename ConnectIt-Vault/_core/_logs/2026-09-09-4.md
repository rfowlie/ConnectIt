---
source: claude-code
run: 2026-09-09
scope: vault
tags: [log, schema,skills]
---

## Request

Introduce the `_core/` machinery drawer; flatten `_logs`; rename `_inbox` → `_clippings`; define the skills model.

## Overview

Created the `_core/` non-domain folder as the home for spec, templates, and vault-wide section instances. Flattened `_logs` to one dated note per run (dropped the `raw/`/`wiki/` + per-source subfolders). Renamed `_inbox` → `_clippings` (Web Clipper framing). Set the two-location skills model: authored copy + `.claude/skills/` mirror.

## Created

—

## Changed

`_logs` schema flattened; `_inbox` → `_clippings` (schema + folder); `_schema/_skills.md`; `_core/` scaffold.

## Flags & Follow-ups

- Reconstructed 2026-09-10 from the `_core/_logs/README.md` index line + on-disk
  evidence; not written at the time (the log-on-change rule landed at `[[2026-09-09-7]]`).
