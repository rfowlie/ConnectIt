---
source: process
run: 2026-09-14-0055
scope: vault
tags:
  - log
---

## Request

`/process` — full vault housekeeping sweep (process-data, process-tasks, process-code,
process-atomic, process-skills).

## Overview

`process-data`: no `_data/` folders exist anywhere in the vault yet — nothing to do.
`process-tasks`: one completed row (`ConnectIt/_tasks/active.md`) moved to its
`complete.md`. `process-code`: the real work this run — checked all 35 `status: current`
pages across all 7 domains; only `ConnectIt` and `UnrealTurnBasedMechanics` had any
commits since their pages' stored provenance (confirmed via `git log <anchor>..HEAD`
per-domain before drilling into individual pages), and both had *substantial* drift from
the SWAP-action work — 7 pages refreshed in total. `process-atomic`: single pool intact
(`_core/_atomic/` only) — nothing to do. `process-skills`: all 16 authored skills
(10 vault-wide + 6 domain) already correctly mirrored — nothing to do.

### process-data

No `_data/` folders found (globbed `**/_data/raw/**`, zero hits). Nothing to process.

### process-tasks

#### 1. Moved

| Folder | Task | From → To |
|---|---|---|
| `ConnectIt/_tasks/` | Verify player loadout's `RootActionClass` is a do-nothing action, not PlacePiece | `active` → `complete` |

`Development/optimal-co-developer/_tasks/` and `_core/_tasks/` — both still placeholder-only, nothing to move.

#### 2. Flags

None — one clean status-driven move, no malformed rows, no archive files needed creating.

### process-code

#### 1. Refreshed

| `code/` folder | page | old commit → new commit |
|---|---|---|
| `ConnectIt` | `AConnectIt_GameMode` | `eddd631` → `131609f` |
| `ConnectIt` | `UConnectIt_BoardStateComponent` | `6477d5d` → `131609f` |
| `ConnectIt` | `UConnectIt_BoardRequestMediator` | `f4cba67` → `131609f` |
| `ConnectIt` | `UConnectIt_BoardRules` | `668872e` → `5047e21` |
| `UnrealTurnBasedMechanics` | `UTurnBasedActionsComponent` | `ae9c1f0` → `131609f` |
| `UnrealTurnBasedMechanics` | `UTurnBasedAction` | `acbfa6f` → `131609f` (unused `#include` removal only — no body rewrite needed) |
| `UnrealTurnBasedMechanics` | `UTurnBasedParticipantManagerComponent` | `3086271` → `1d1c70f` |

Both domains' `code/__INDEX.md` anchor `commit:`/`reconciled:` also bumped
(`ConnectIt` → `efc8052`, `UnrealTurnBasedMechanics` → `131609f`) to the true latest
commit touching each module (broader than the per-page anchors above, which reflect only
each page's own `source:` paths).

#### 2. Unchanged

28 of 35 `current`-status pages checked and confirmed still current: 5 more in `ConnectIt`
(`AConnectIt_GameState`, `UConnectIt_BoardRegistrySubsystem`, `ConnectItStructs`,
`UConnectIt_State_Game`, `UConnectIt_GameFacade`), 4 more in `UnrealTurnBasedMechanics`
(`UTurnBasedActionBase`, `UActionLoadoutDataAsset`, `UTurnBasedParticipantComponent`,
`ITurnOrderInterface`), and all 19 `current` pages across `UnrealGridMechanics` (6),
`UnrealGameMechanics` (6), `UnrealGameIntelligence` (4), `UnrealCodingUtils` (1), and
`UnrealAIMechanics` (2) — zero commits touched any of those five domains' source since
their pages' anchors, confirmed via `git log <anchor>..HEAD` per domain before checking
individual pages.

#### 3. Flags

- **`ITurnBasedActionNotifier` (+ `FTurnBasedActionNotifierDelegate`) has no inventory
  row at all** in `UnrealTurnBasedMechanics/code/__INDEX.md` — a real, committed type
  (`Plugins/.../Action/Interface/TurnBasedActionNotifier.{h,cpp}`) missing from the
  inventory entirely, not just marked `stub`. Per this skill's own rule, not adding a row
  on its own initiative — flagging for the next `/process-code` run or a manual add.
  `AConnectIt_HUD` (ConnectIt, `stub`) is the only current implementer.
  `ITurnBasedActionNotifier`/`RegisterActionNotifier` is not called from anywhere yet.
- **`ConnectIt/code/__INDEX.md`'s "mid–board-architecture-overhaul" framing paragraph is
  now stale** — the board-architecture work it describes is done; the SWAP action work is
  the current phase and isn't reflected in that prose at all (only the anchor
  commit/date were mechanically updated this run, not the characterization — that's a
  judgment call beyond this skill's per-page drift charter).
- No `stale`-status or `stub`-with-touched-source rows needed a status flip — every
  refreshed page was already `current`.

### process-atomic

Only `_core/_atomic/` exists anywhere in the vault (globbed `atomic/`/`_atomic/` at any
depth). Nothing to merge.

### process-skills

#### 1. Mirrored

All 16 authored skills (10 vault-wide `_core/_skills/`, 6 domain: 2
`Development/_skills/`, 1 `Development/board-of-advisors/_skills/`, 3
`Development/optimal-co-developer/_skills/`) diffed byte-for-byte against their
`.claude/skills/<name>/SKILL.md` mirror — all 16 already in sync. Nothing created,
nothing refreshed.

#### 2. Flags

None — no missing `name:` frontmatter, no orphan mirrors, no name collisions.

## Summary

- process-data: nothing to do (no `_data/` folders exist).
- process-tasks: 1 row moved (`ConnectIt/_tasks/`).
- process-code: 7 of 35 `current` pages refreshed (all in `ConnectIt` +
  `UnrealTurnBasedMechanics`, the only two domains touched since their last reconciliation);
  2 flags raised (missing inventory row, stale index framing).
- process-atomic: nothing to do (single pool already intact).
- process-skills: nothing to do (16/16 already mirrored).

## Created

None.

## Changed

**process-tasks:**
- [`ConnectIt/_tasks/active.md`](../../ConnectIt/_tasks/active.md) — row removed.
- [`ConnectIt/_tasks/complete.md`](../../ConnectIt/_tasks/complete.md) — row appended, `Moved: 2026-09-14`.

**process-code:**
- [`ConnectIt/code/AConnectIt_GameMode.md`](../../ConnectIt/code/AConnectIt_GameMode.md)
- [`ConnectIt/code/UConnectIt_BoardStateComponent.md`](../../ConnectIt/code/UConnectIt_BoardStateComponent.md)
- [`ConnectIt/code/UConnectIt_BoardRequestMediator.md`](../../ConnectIt/code/UConnectIt_BoardRequestMediator.md)
- [`ConnectIt/code/UConnectIt_BoardRules.md`](../../ConnectIt/code/UConnectIt_BoardRules.md)
- [`ConnectIt/code/__INDEX.md`](../../ConnectIt/code/__INDEX.md) — anchor bumped + one-line note added
- [`UnrealTurnBasedMechanics/code/UTurnBasedActionsComponent.md`](../../UnrealTurnBasedMechanics/code/UTurnBasedActionsComponent.md)
- [`UnrealTurnBasedMechanics/code/UTurnBasedAction.md`](../../UnrealTurnBasedMechanics/code/UTurnBasedAction.md) — provenance only, no body change
- [`UnrealTurnBasedMechanics/code/UTurnBasedParticipantManagerComponent.md`](../../UnrealTurnBasedMechanics/code/UTurnBasedParticipantManagerComponent.md)
- [`UnrealTurnBasedMechanics/code/__INDEX.md`](../../UnrealTurnBasedMechanics/code/__INDEX.md) — anchor bumped

**process-data / process-atomic / process-skills:** nothing changed.

## Flags & Follow-ups

Carried up from process-code (the only sub-skill that raised any):

- `ITurnBasedActionNotifier` needs an inventory row in
  `UnrealTurnBasedMechanics/code/__INDEX.md` (currently missing entirely, not just
  `stub`) — a job for the next `/process-code` run or a manual add, not this one.
- `ConnectIt/code/__INDEX.md`'s "mid–board-architecture-overhaul" framing paragraph
  should get a real rewrite (not just the anchor bump done here) once the SWAP action
  work settles — a content judgment call, not mechanical drift-fixing.
