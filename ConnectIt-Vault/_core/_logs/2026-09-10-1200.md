---
source: claude-code
run: 2026-09-10-1200
scope: vault
tags: [log, migration, old, decisions, extraction]
---

## Request

Item 4 of the `_core/` migration — assess `old/` (26 `.md` files extracted from the
project source tree: `Plugins/*/Docs/`, `Source/ConnectIt/Docs/`) for log history, code
architecture, and gotchas; place anything salvageable into vault notes or new notes; then
repoint any links into `old/` and delete it.

## Overview

- **`ConnectIt/decisions/` created** (new section, per `_core/_schema/_decisions.md`)
  with an `index.md` and five dated notes:
  - `2026-09-06-suite-conventions.md` — tags-drive-sequencing, no `Client_`/`Multicast_`
    RPCs, the `FInstancedStruct Payload` extension hook, `UDWidgetBase` family, the
    non-uniform `_Native` delegate convention. From `architecture/Overview.md` +
    `Conventions.md`.
  - `2026-09-08-board-architecture-overhaul.md` — retire `AConnectIt_BoardManager`; split
    into `UConnectIt_BoardRequestMediator` + `UConnectIt_BoardRules` (server-only on
    GameMode), `UConnectIt_BoardStateComponent` (on GameState), `UConnectIt_LevelConfig*`;
    the whole shift pipeline confirmed dead + removed. Outstanding manual editor steps
    listed. From `ConnectItModule.md`.
  - `2026-09-08-retire-legacy-mvvm-pipeline.md` — keep the state-machine / facade /
    view-model set as documented dead code, don't build on it; the legacy→live mapping.
    From `LegacyPipeline.md` + `Duplication.md` #1.
  - `2026-09-09-board-registries-to-world-subsystem.md` — registries off the controller
    onto `UConnectIt_BoardRegistrySubsystem`, `DuplicateObject`'d per world from
    `Instanced` level-config templates; the "full symmetric fix" scope choice. From
    `ConnectItModule.md` "second pass".
  - `2026-09-09-faction-visuals-subsystem-built-then-removed.md` — built the
    `UGameInstanceSubsystem` + palette + `UDeveloperSettings`, then removed at owner
    request (white-box phase); design intent recorded for later. From
    `UIValueCatalogue.md` §H.
- **`ConnectIt/code/index.md` `## Known issues`** — the `Duplication.md` structural
  register (legacy pipeline, two MinMax impls, `AConnectIt_GridPiece` dead replication,
  parked scaffolds, unprefixed influence-map classes) + the 2 confirmed correctness bugs
  (`ServerRouteBoardChangeRequest_Implementation` FactionID spoof;
  `PieceControlInfluenceMap::Update()` vs never-populated `FactionTurn`) + a pointer to
  the plugin-side overlap entries.
- **`ConnectIt/code/runtime-state.md`** — the by-system accessor tables from
  `RuntimeStateAccess.md` (board / turn-participant-match / action-stack / sequencing).
- **Per-domain gotchas woven in** — `Create()` vs `NewObject(owner)` GC ownership →
  `UnrealGameMechanics/code/UGameEventTaskManager.md`; composite influence-map patterns
  (managed construction vs dependency injection, rewritten against the real
  `IGI_FloatGridDataProviderInterface`, discarding the phantom `IGI_InfluenceMap`
  samples) → `UnrealGameIntelligence/code/recipes/add-an-influence-map-visualiser.md`.
  The `TMinMaxManager` trail, the two non-functional grid editor validators, the
  `UGridTrackerSubsystem`/`UGridHoverSubsystem` overlap, and the retired
  `BeginResolutionHold` counter were already captured in the domain `CLAUDE.md` rough
  edges during the 0900 re-ingest.
- **`old/` deleted** — 26 files. Not re-imported (per triage): `AConnectIt_BoardManager`,
  `BeginResolutionHold`/`EndResolutionHold`, `QueueTagSequence`, the ConnectIt shift
  pipeline, `IGridBoardManagerInterface`, `SubsystemDiscovery-DualAccessPattern.md`, the
  phantom `IGI_InfluenceMap` API, the six `Workflows/*.md` (already reflected in the
  `systems/` pages).

## Created

- `ConnectIt/decisions/` — `index.md` + 5 dated notes (above)
- `ConnectIt/code/runtime-state.md`

## Changed

- `ConnectIt/code/index.md` — `## Known issues`, `## Map` cross-cutting-conventions
  pointer, Related → `runtime-state.md` + `decisions/`
- `ConnectIt/CLAUDE.md` — already referenced `decisions/` (written in the 0900 session);
  the notes it pointed at now exist
- `UnrealGameMechanics/code/UGameEventTaskManager.md`,
  `UnrealGameIntelligence/code/recipes/add-an-influence-map-visualiser.md` — gotchas /
  patterns woven in

## Deleted

- `old/` — all 26 `.md` files + the `Plugins/` and `Source/` subtrees under it

## Flags & Follow-ups

- Two immutable `ConnectIt/logs/` notes (`2026-09-05.md`, `2026-09-09.md`) still mention
  "`../old/Source/ConnectIt/Docs/`" in prose — accurate history of what those passes did;
  left per the log-immutability rule. They are prose, not links.
- The `## Known issues` entries are recorded, not triaged — most need an owner decision or
  a C++ change out of scope here.
- `ideas.md` at the vault root is still untriaged (flagged out-of-scope in the plan).
