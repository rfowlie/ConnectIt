---
source: process
run: 2026-09-18-0154
scope: vault
tags:
  - log
---
## Request

User ran `/process` — the full sweep (`process-data`, `process-tasks`, `process-code`,
`process-atomic`, `process-skills`, in that order).

## Overview

First `process-code` run since 2026-09-14 — 4+ days of real code (SWAP shipping, Board
Shift, the `ActionTag`→`GetActionTag()` refactor, the InputTagBinder rewrite) had
accumulated undocumented drift across two plugins and ConnectIt. Everything else in the
sweep was clean: no `_data/` folders exist yet (`process-data` no-op), all three
`_tasks/active.md` hold only `active` rows (`process-tasks` no-op), the atomic pool has no
duplicates and no `_data/wiki/` to sweep (`process-atomic` both passes no-op), and all 15
authored skills already match their `.claude/skills/` mirrors exactly (`process-skills`
no-op).

`process-code` found 21 pages with real git-history drift against their `commit:` anchor.
Refreshed all of them — several needed real content fixes (a stale API reference, missing
struct fields, a new capability undocumented entirely); most were additive/internal
changes needing only frontmatter bump + a one-line `## Changes` note. Four pages
(`add-an-influence-map-visualiser.md`, `gate-a-response-on-a-tag.md`, `game-state-machine.md`,
`UConnectIt_State_Game.md`) couldn't be path-verified by this run's tooling and are
flagged, not confirmed either way.

**One thing surfaced during this run, unrelated to any of the five skills**: while process-code
was in progress, `Source/ConnectIt/Public/Board/ConnectIt_PieceRegistry.h` changed on disk
— `HandleGameEventComplete`'s `UFUNCTION()` became `UFUNCTION(BlueprintImplementableEvent, ...)`.
That's a real conflict with the C++ implementation already sitting in the matching `.cpp`
(from this session's earlier registry-mapping-fix work) — `BlueprintImplementableEvent`
means Blueprint supplies the body, not C++. Flagged prominently below; not touched by this
sweep, which only writes to `code/` pages and `.claude/skills/`.

## Created

None.

## Changed

**process-code** — `ConnectIt/code/`:
- `ConnectItStructs.md` — added `bCanShift`, the Board Shift `FConnectItBoardChangeEvent`
  fields, `FConnectItRequestBoardShift`. Real content fix.
- `UConnectIt_BoardRequestMediator.md` — added `HandleBoardShiftRequest` to Entry points +
  a Changes entry. Real content fix.
- `runtime-state.md`, `systems/board-state-single-source-of-truth.md`,
  `systems/place-piece-request.md`, `recipes/add-a-board-request-type.md`,
  `recipes/add-a-scoring-rule.md` — frontmatter bump (+ a one-line Changes note on
  `runtime-state.md`); content already accurate or the drift was internal-only.

**process-code** — `UnrealTurnBasedMechanics/code/`:
- `UTurnBasedActionBase.md` — added `GetActionTag()` as the actual Entry point, replacing
  the stale `ActionTag` framing. Real content fix.
- `UTurnBasedAction.md` — fixed a since-removed API reference
  (`OnBoundInputTriggered`/shared broadcast → `ConstructInputBindings()`/per-binding
  delegate). Real content fix.
- `UActionLoadoutDataAsset.md`, `UTurnBasedActionsComponent.md` — frontmatter + Changes
  note (internal-only `ActionTag`→`GetActionTag()` rename, no public behavior change).
- `recipes/add-a-turn-action.md`, `systems/action-stack-lifecycle.md`,
  `systems/turn-end-tag-gate.md` — frontmatter bump only; content already accurate.

**process-code** — `UnrealGameMechanics/code/`:
- `UInputTagBinder.md` — frontmatter bump only; content already fully accurate (someone
  had kept this page in sync outside the vault-log discipline).

**process-code** — `UnrealGridMechanics/code/`:
- `UGridMechanics_GridLibrary.md` — added `RotateGridDirection`. Real content fix.
- `UBoardShiftComponent.md`, `systems/board-shift.md` — added a prominent note that
  ConnectIt's own Board Shift action does **not** use this plugin pipeline (no diagonal
  support) — a real, previously-undisclosed divergence, not just staleness.

**process-skills / process-atomic / process-tasks / process-data** — nothing changed
(confirmed no-op each, see Overview).

## Flags & Follow-ups

- **`ConnectIt_PieceRegistry.h`'s `HandleGameEventComplete` UFUNCTION specifier changed to
  `BlueprintImplementableEvent`** while this sweep was running — conflicts with the C++
  body already in `ConnectIt_PieceRegistry.cpp`. Not part of this sweep's scope to fix;
  surfaced to the user directly, not silently corrected.
- Four pages could not be path-verified by this run's drift-check tooling and are
  unconfirmed either way: `UnrealGameIntelligence/code/recipes/add-an-influence-map-visualiser.md`,
  `UnrealGameMechanics/code/recipes/gate-a-response-on-a-tag.md`,
  `ConnectIt/code/systems/game-state-machine.md`, `ConnectIt/code/UConnectIt_State_Game.md`.
  Worth a manual check next `/process-code` run.
- `ConnectIt/code/UConnectIt_BoardRegistrySubsystem.md` and
  `UnrealGameMechanics/code/UGameEventTaskSubsystem.md` were already refreshed earlier
  this session (registry-mapping-fix work) — not re-touched by this sweep, still carry
  the "uncommitted, re-anchor once committed" flag from that pass.
- None of the refreshed pages' `commit:` anchors reflect this session's own uncommitted
  registry-mapping-fix changes (per the above) — only real git history, per the schema's
  own provenance model.
