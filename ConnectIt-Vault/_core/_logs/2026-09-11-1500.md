---
source: claude-code
run: 2026-09-11-1500
scope: vault
tags:
  - log
---

## Request

Three asks in `Development/optimal-co-developer/`: (1) initialize schema folders for
meetings/questions/decisions/tasks/daily, (2) add a new `/ask-partner` skill, (3) decide
whether an "always be the co-developer persona unless a skill is running" directive
belongs in a root `CLAUDE.md`. Scoping (2) surfaced that `optimal-co-developer/_skills/`
already existed underscore-prefixed while the vault's documented rule at the time said
live domain sections stay bare — the user's call: reverse the rule vault-wide instead of
treating the folder as an exception. Confirmed: the persona directive goes in
`Development/CLAUDE.md`; `/ask-partner` is a single-voice counterpart to `/ask-the-board`;
`/process-skills` should be extended to also mirror domain `<domain>/_skills/*.md` files
(it only covered `_core/_skills/*.md`, which is why `ask-the-board`'s mirror was missing
despite `board-of-advisors/CLAUDE.md` claiming it existed).

## Overview

- **Naming rule reversed.** Every schema-governed section folder — domain-level or
  vault-wide — now gets the `_` prefix (previously: `_` meant `_core/`/`_schema/` only,
  live domain sections were bare). `_code`, `_atomic`, `_maps`, `_user` are explicitly
  deferred, not touched.
- **5 physical renames** (`git mv`, history preserved):
  `board-of-advisors/questions/` → `_questions/`, `board-of-advisors/skills/` →
  `_skills/`, `board-of-advisors/knowledge/` → `_knowledge/`, `ConnectIt/decisions/` →
  `_decisions/`, `ConnectIt/logs/` → `_logs/`. `optimal-co-developer/_skills/` was
  already correct — no-op.
- **Rule text flipped** in `_core/_schema/__README.md`'s naming-conventions bullet and in
  every section rulebook's mount/location lines and `Deviations` instructions:
  `_tasks.md`, `_questions.md`, `_decisions.md`, `_meetings.md`, `_daily.md`,
  `_skills.md` (its `<domain>/skills/<name>.md` row too), `_logs.md`, `_people.md`,
  `_data.md`, `_clippings.md`, `_attachments.md`, `_core.md`.
- **Every reference to a renamed path fixed** — `_core/_templates/` (`TSchemaMeetings.md`,
  `TSchemaSkills.md`, `TSchema.md`'s stale `README.md`→`__README.md` mention),
  `_core/_skills/` (`fix-links.md`, `ask-vault.md`, `process-data.md`, `process-tasks.md`,
  `__README.md`), the vault-wide section `__README.md`/`__INDEX.md` files
  (`_ask-vault/`, `_attachments/`, `_clippings/`, `_questions/`, `_tasks/`, `_logs/`),
  `Development/board-of-advisors/CLAUDE.md` + `board-of-advisors.md` +
  `_skills/ask-the-board.md` (every `knowledge/`/`questions/` mention, plus a
  `README.md`→`__README.md` fix), the 3 advisor wiki notes' raw-material line, and
  `ConnectIt/CLAUDE.md`'s decisions/logs mentions.
- **34 `[[wikilinks]]` retargeted** under `ConnectIt/` from `ConnectIt/decisions/…` (28) /
  `ConnectIt/logs/…` (6) to the `_`-prefixed paths — alias text left untouched except the
  one `[[ConnectIt/_logs/__INDEX|logs/]]` case in `_decisions/__INDEX.md`, whose display
  label was also updated to `_logs/`. `ConnectIt/_logs/__INDEX.md`'s own `# ConnectIt/logs/`
  heading was fixed the same way. Confirmed zero remaining `ConnectIt/decisions/` or
  `ConnectIt/logs/` targets; every retargeted link resolves to a real file.
- **`optimal-co-developer` schema folders built out**: `_meetings/`, `_questions/`,
  `_decisions/`, `_daily/` each seeded with a one/two-sentence `__README.md` + governed-by
  link; `_tasks/` additionally seeded with `active.md` / `suspended.md` / `complete.md`
  matching the worked `_core/_tasks/` example.
- **`/ask-partner` skill added** (`optimal-co-developer/_skills/ask-partner.md`),
  structurally modeled on `ask-the-board.md`'s real section shape (not the generic
  `TSchemaSkills` template): reads `co-developer/identity.md` + `soul.md` for role and
  voice, `me/user.md` + `me/career-coach.md` for the owner's real situation, answers in
  first person in `soul.md`'s voice with a "My take" / "Where I'd push back" / "What I'd
  actually do" shape, commits to a position rather than surveying, and logs each
  consultation to `_questions/`.
- **`optimal-co-developer/CLAUDE.md` fixed**: the file table's `identity.md`/`soul.md`/
  `user.md` links now point at their real paths (`co-developer/` vs `me/` — added a
  one-line note on the split), a new "Sections" table lists the 5 new folders + `_skills/`,
  and the "Extraction caveat" paragraph now reflects reality (`career-coach.md` is local
  under this domain's own `me/`, alongside `user.md` — no cross-repo link risk).
- **`Development/CLAUDE.md`** gained a new "## Default posture" section (after "Who this
  is for", before "Sub-projects"): always answer as the co-developer defined in
  `co-developer/soul.md` + `identity.md` unless a skill's own instructions define a
  different output format/role/voice for the task at hand, in which case follow the
  skill's instructions for that output and resume the default posture after. Also applied
  the reference-prefix fix to this file's own bare mentions (Sub-projects table,
  Conventions' `knowledge/` bullets, Start-of-session steps 3/4/6, the "Where new
  information goes" table, Guardrails' `knowledge/raw/`/`logs/raw/` lines) — the dead
  `_schema/README.md` "Structure" section and the already-broken `optimal-co-developer/
  USER.md` href were left untouched (pre-existing, unrelated).
- **`/process-skills` extended** to cover domain skills: a 4th "Inputs to read first" item
  for every `<domain>/_skills/*.md`; Steps split into Pass 1 (vault-wide, existing logic)
  and Pass 2 (domain skills, same logic, flags rather than silently resolves a `name:`
  collision); the Log step now additionally groups domain-skill changes by domain into
  that domain's own `_logs/`. Ran it by hand afterward: created
  `.claude/skills/ask-the-board/SKILL.md` (long-missing despite
  `board-of-advisors/CLAUDE.md` claiming it existed) and
  `.claude/skills/ask-partner/SKILL.md`; refreshed the mirrors for the 5 vault-wide
  skills whose authored copies changed in this pass or the prior link-format session
  (`ask-vault`, `fix-links`, `process-data`, `process-skills`, `process-tasks`) — all now
  byte-identical to their authored source. `process-atomic` and `process` were already in
  sync, untouched.
- **Grep sweep for stragglers** (`_core`, `Development`, `ConnectIt`, the 10 renamed
  section names): clean after fixing several cross-references the plan's own file list
  didn't spell out line-by-line — assorted `data/`, `decisions/`, `people/`, `meetings/`,
  `logs/` mentions inside the schema rulebooks themselves (each rulebook cross-refers its
  neighbours), plus `ConnectIt/_logs/__INDEX.md`'s own `# ConnectIt/logs/` heading (see
  Changed). Remaining hits are all expected: the out-of-scope `_user.md`/`_atomic.md`/
  `_maps.md`/`_code.md` schema files, every domain's `code/` folder, historical dated
  `_logs/`/`_decisions/` prose (append-only, not rewritten), and this file's own Changelog
  history.

## Created

- `Development/optimal-co-developer/_meetings/__README.md`
- `Development/optimal-co-developer/_questions/__README.md`
- `Development/optimal-co-developer/_decisions/__README.md`
- `Development/optimal-co-developer/_daily/__README.md`
- `Development/optimal-co-developer/_tasks/__README.md` + `active.md` + `suspended.md` +
  `complete.md`
- `Development/optimal-co-developer/_skills/ask-partner.md`
- `.claude/skills/ask-the-board/SKILL.md`
- `.claude/skills/ask-partner/SKILL.md`

## Changed

- `_core/_schema/__README.md` — naming-conventions bullet reversed
- `_core/_schema/_tasks.md`, `_questions.md`, `_decisions.md`, `_meetings.md`,
  `_daily.md`, `_skills.md`, `_logs.md`, `_people.md`, `_data.md`, `_clippings.md`,
  `_attachments.md`, `_core.md` — mount/location lines + `Deviations` `_`-prefixed
- `_core/_templates/TSchemaMeetings.md`, `TSchemaSkills.md`, `TSchema.md`
- `_core/_skills/fix-links.md`, `ask-vault.md`, `process-data.md`, `process-tasks.md`,
  `__README.md`, `process-skills.md` (extended for domain skills)
- `_core/_ask-vault/__README.md`, `_attachments/__README.md`, `_clippings/__README.md`,
  `_questions/__README.md`, `_tasks/__README.md`, `_logs/__INDEX.md`
- `Development/board-of-advisors/CLAUDE.md`, `board-of-advisors.md`,
  `_skills/ask-the-board.md`, `_knowledge/wiki/{jonas-tyroller,tim-cain,tim-ruswick}.md`
- `ConnectIt/CLAUDE.md`, `ConnectIt/_decisions/__INDEX.md` (alias-label fix),
  `ConnectIt/_logs/__INDEX.md` (heading fix) + the 34 retargeted wikilinks across
  `code/AConnectIt_GameMode.md`, `code/systems/game-state-machine.md`,
  `code/UConnectIt_BoardRegistrySubsystem.md`, `code/UConnectIt_GameFacade.md`,
  `code/UConnectIt_State_Game.md`, `code/__INDEX.md`, 3 `_decisions/` notes,
  `_decisions/__INDEX.md`, `_logs/2026-09-04-2.md`, `_logs/2026-09-04.md`,
  `_logs/__INDEX.md`
- `Development/optimal-co-developer/CLAUDE.md` — file-table links, new Sections table,
  extraction-caveat correction
- `Development/CLAUDE.md` — new "Default posture" section + reference-prefix fixes
- `ABOUT.md` — section table, Conventions, Open questions, Changelog

## Flags & Follow-ups

- **`_user`, `_atomic`, `_maps`, `_code` schemas and every domain's `code/` folder stay
  bare by design** — deliberately deferred from this reversal; a separate future pass if
  full consistency across every section kind is wanted.
- **`board-of-advisors/_knowledge/` keeps its `knowledge` label** rather than being renamed
  to match the `_data` schema's actual name — a pre-existing mismatch, unrelated to this
  pass; its `__README.md`s still link to a dead `_schema/knowledge.md`.
- **Broken cross-references to `career-coach.md`/`user.md` under
  `board-of-advisors/_knowledge/me/`** — the real files are local at
  `optimal-co-developer/me/career-coach.md` / `me/user.md`. Pre-existing (already flagged
  above in Open questions); the `_` prefix was applied mechanically to these references
  without fixing the underlying dead target, per the user's explicit scoping.
- **The outer game-repo's duplicate `C:\...\ConnectIt\.claude\skills\ask-the-board\SKILL.md`**
  is unrelated to the vault-scoped `ConnectIt-Vault/.claude/skills/` mirror created this
  session — already known-stale per Open questions above, left alone.
- **`Development/CLAUDE.md`'s "Structure: how folders are governed" section** still points
  at a dead `_schema/README.md` and stale example links — pre-existing, unrelated to this
  pass, left untouched.
- `/process-skills`'s new Pass-2 domain-logging behavior (grouping domain-skill changes
  into `<domain>/_logs/`) wasn't exercised by creating `board-of-advisors/_logs/` or
  `optimal-co-developer/_logs/` on this manual run — this single vault-scope entry covers
  the whole session's change-set instead, per the plan's own standing rule to log once,
  last. A future real `/process-skills` invocation will create those domain logs itself
  the first time it has domain-skill changes to report.
