---
source: session
run: 2026-09-16-1609
scope: vault
tags:
  - log
---

## Request

Owner asked that `/session-open`'s map output be more direct and bullet-pointed going
forward, rather than the prose-paragraph style it was using.

## Overview

Rewrote `Development/_skills/session-open.md`'s "Output — the map" and "Style" sections:
each of the five blocks is now explicitly a bullet list (one fact per bullet, no
connective/scene-setting prose), and the "Then create the session note" template was
updated to match (each `**Label:**` followed by its own bullet list instead of one inline
sentence). Re-mirrored to `.claude/skills/session-open/SKILL.md`. Applies to future
`/session-open` runs only — the existing `ConnectIt/_sessions/2026-09-16-1549.md` note was
left in its original prose form, not retroactively reformatted.

## Created

None.

## Changed

- [`Development/_skills/session-open.md`](../../Development/_skills/session-open.md) —
  "Output — the map" (all 5 blocks), the session-note template block, and "Style" rewritten
  for bullet-list output.
- [`.claude/skills/session-open/SKILL.md`](../../.claude/skills/session-open/SKILL.md) —
  re-mirrored to match.

## Flags & Follow-ups

- `session-close.md` wasn't touched — its own reckoning output format wasn't part of this
  request. Worth asking the owner whether the same bullet-first preference should extend
  there too, rather than assuming it.
- Saved as a standing preference in Claude's cross-session memory
  (`session-format-preference.md`) so future sessions default to this without needing the
  instruction repeated, since the skill file itself only governs `/session-open` runs
  inside this vault.
