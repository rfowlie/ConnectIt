---
source: /process (manual read-and-execute — no .claude/ mirror existed yet at start of run)
run: 2026-09-11-0112
scope: vault
tags:
  - log
---

## Request

Verify the `process-*` skill family is registered and functional, then run `process.md`'s
full sweep against this vault to bring it up to date.

## Overview

**Registration check.** Every skill is correctly catalogued in `_core/_skills/__INDEX.md`
(all 6 `process-*` files + `ask-vault`, matching disk exactly) and correctly wired into
the schema it enforces (`_schema/_tasks.md`, `_code.md`, `_data.md`, `_skills.md`,
`_atomic.md` each name their skill by slash-command; `_clippings.md` documents its
intentional non-coverage). The one gap found: **no skill was actually invocable as a
`/slash-command`** — this vault had no `.claude/` directory at all, so none of the 6
`process-*` skills nor `ask-vault` had ever been mirrored. This is a known, self-documented
bootstrapping gap (`_core/_logs/2026-09-10-2.md` flagged it at creation time), not new
breakage. Fixed as part of running `process-skills` below.

**Full sweep**, run in discovery order (data → tasks → code → atomic → skills):

### process-data
No `data/raw/` folders exist anywhere in this vault (the one raw-material folder,
`Development/board-of-advisors/knowledge/raw/`, is a different, unrelated schema shape).
Nothing to do.

### process-tasks
Only one `tasks/active.md` exists in the vault (`_core/_tasks/active.md`), holding a
single placeholder row (`_(none yet — add rows here)_`). Nothing to move.

### process-code
Checked all 35 pages across the 8 domains' `code/__INDEX.md` inventories marked
`status: current` (`stub` and `covered in X` rows correctly skipped, per the skill's own
rule). For every one, `git log -1 --format=%h -- <its source: paths>` matched the page's
stored `commit:` exactly, and every `source:` path resolves on disk — **zero drift, zero
missing paths**. No page needed refreshing.

### process-atomic
No `atomic/` or `_atomic/` folder exists anywhere except `_core/_atomic/` itself. Nothing
to merge.

### process-skills
No `.claude/skills/` directory existed. Created `.claude/skills/<name>/SKILL.md` for all
7 vault-wide skills, verbatim from their authored `_core/_skills/` copies — this vault's
skills are now invocable as `/ask-vault`, `/process`, `/process-data`, `/process-tasks`,
`/process-code`, `/process-atomic`, `/process-skills`.

## Summary

- process-data: nothing to do (no `data/raw/` in this vault)
- process-tasks: nothing to do (only placeholder row)
- process-code: 35 pages checked, 0 drifted, 0 missing source paths
- process-atomic: nothing to do (no offending folders)
- process-skills: 7 mirrors created (all previously missing)

## Created

- `.claude/skills/ask-vault/SKILL.md`
- `.claude/skills/process/SKILL.md`
- `.claude/skills/process-data/SKILL.md`
- `.claude/skills/process-tasks/SKILL.md`
- `.claude/skills/process-code/SKILL.md`
- `.claude/skills/process-atomic/SKILL.md`
- `.claude/skills/process-skills/SKILL.md`

## Changed

_(none — every sub-skill this run either found nothing to do, or only created new files)_

## Flags & Follow-ups

- This run was executed by reading and following each skill's markdown directly, since
  no `/process` command existed yet at the start of it — now that the mirrors exist,
  future runs can invoke `/process` for real.
- `process-tasks`'s own glob (`**/tasks/active.md`) technically doesn't match
  `_core/_tasks/active.md` (folder is `_tasks`, not `tasks`) even though the skill's
  prose explicitly calls that file out by name — checked it directly instead of relying
  on the glob. Worth tightening the skill's own glob pattern in a future pass; left
  as-is here since it's a pre-existing skill-text issue, not part of this run's scope.
