---
source: claude-code
run: 2026-09-09
scope: vault
tags: [log, core,routing]
---

## Request

Give `_core/` its own module router; reduce the root `CLAUDE.md` to a thin stub.

## Overview

Added `_core/CLAUDE.md` — start-of-session pointers, the standing rules (log every change, keep `ABOUT.md` current), and how to drop `_core/` into another vault. Root `CLAUDE.md` reduced to an `@_core/CLAUDE.md` import plus a short vault framing, since a nested `CLAUDE.md` only auto-loads once a file inside it is touched.

## Created

`_core/CLAUDE.md`.

## Changed

`CLAUDE.md` (root) → `@import` stub.

## Flags & Follow-ups

- Reconstructed 2026-09-10 from the `_core/_logs/README.md` index line + on-disk
  evidence; not written at the time (the log-on-change rule landed at `[[2026-09-09-7]]`).
