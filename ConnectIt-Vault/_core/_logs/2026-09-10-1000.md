---
source: claude-code
run: 2026-09-10-1000
scope: vault
tags: [log, migration, architecture]
---

## Request

Fold `architecture/` into `ConnectIt/` (item 7 of the `_core/` migration) — the suite
dependency graph + layering + enabled-vs-not go into the `## Map` section of
`ConnectIt/code/index.md`, per the user's "ConnectIt/code/index.md Map" choice.

## Overview

- **`architecture/Overview.md` content folded** into a new `## Map` section of
  `ConnectIt/code/index.md`: the dependency graph (ASCII), the per-plugin layering
  paragraph, "enabled vs not", "where to start reading", and the cross-cutting
  conventions — reworded as *the suite seen from the game domain* (the `_code` schema
  makes `index.md` the architecture MOC).
- **`architecture/` deleted** (folder + its already-removed `_section.md` +
  `overview.md`). Git tracked the file lowercase (`overview.md`) though disk showed
  `Overview.md` — a Windows case slip; removed via the git-tracked path.
- The old `[[Overview|architecture/overview.md]]` link in `ConnectIt/code/index.md`'s
  Related section is gone (the whole file was rewritten this pass — see the code
  re-ingest note).
- `architecture/`'s "Cross-cutting conventions" and "Known suite-level tensions" seeded
  `ConnectIt/decisions/2026-09-06-suite-conventions.md` and the `## Known issues`
  section of `ConnectIt/code/index.md` (see the `old/` extraction note).

## Created

- `ConnectIt/code/index.md` `## Map` section (part of the full rewrite)

## Changed

- `ConnectIt/code/index.md` — rewritten (frontmatter, preamble, inventory, `## Map`,
  `## Known issues`, `## Related`)

## Deleted

- `architecture/overview.md`, `architecture/_section.md`, `architecture/` folder

## Flags & Follow-ups

- No `architecture/` or `[[Overview` references remain in `*.md` outside `ABOUT.md`'s
  changelog and this log family.
