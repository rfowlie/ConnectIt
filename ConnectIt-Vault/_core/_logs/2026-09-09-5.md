---
source: user
run: 2026-09-09
scope: vault
tags: [log, schema]
---

## Request

Move `_schema/` inside `_core/` (as `_core/_schema/`); doc reconciliation left pending.

## Overview

User relocated the whole `_schema/` folder into `_core/` and `_`-prefixed every rulebook (`_code.md`, `_logs.md`, …). The vault's routers, `_section.md` files, and `[[_schema/…]]` wikilinks were left pointing at the old location — reconciliation deferred (done 2026-09-10, `[[2026-09-10-0900]]`).

## Created

—

## Changed

`_schema/` → `_core/_schema/`; rulebooks `_`-prefixed.

## Flags & Follow-ups

- Reconstructed 2026-09-10 from the `_core/_logs/README.md` index line + on-disk
  evidence; not written at the time (the log-on-change rule landed at `[[2026-09-09-7]]`).
