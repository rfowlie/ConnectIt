---
source: claude-code
run: 2026-09-09
scope: vault
tags: [log, schema]
---

## Request

Schema-model revision: consolidate the code-doc sections, drop several, add task/question/attachment sections, reframe `_core/`.

## Overview

Consolidated `_systems` + `_recipes` + `_updates` into `_code` (one `code/` folder with per-type pages, nested `systems/`/`recipes/`, and a per-page `## Changes` history). Dropped `_frameworks` (its job absorbed by `code/index.md`'s Map). Added `_tasks`, `_questions`, `_attachments`. Reframed `_core/` as the vault's single non-domain folder holding spec + templates + vault-wide section instances.

## Created

`_schema/_tasks.md`, `_schema/_questions.md`, `_schema/_attachments.md`; `_core/_tasks/`, `_core/_questions/`, `_core/_attachments/`.

## Changed

`_schema/README.md` catalogue rewritten; `_code.md` rewritten; `_systems.md`/`_recipes.md`/`_updates.md`/`_frameworks.md` removed; `_tasks.md`/`_questions.md`/`_attachments.md` added; `_core.md` reframed.

## Flags & Follow-ups

- Reconstructed 2026-09-10 from the `_core/_logs/README.md` index line + on-disk
  evidence; not written at the time (the log-on-change rule landed at `[[2026-09-09-7]]`).
