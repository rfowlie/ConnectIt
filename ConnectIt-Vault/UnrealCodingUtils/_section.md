---
type: code-unit
lead: high-level.md
schemas: [updates, code]
---

# UnrealCodingUtils — section

Dependency-free C++ helper grab-bag plugin. Lead doc: [[UnrealCodingUtils/high-level|high-level.md]].

Governed subfolders:

- `updates/` → [[_schema/updates|_schema/updates.md]] (created on first use)
- `code/` → [[_schema/code|_schema/code.md]] — see [[UnrealCodingUtils/code/index|code/index.md]]

No `systems/` or `recipes/` — the plugin has no multi-type flow or task worth a page yet.
Add them to `schemas` if that changes.
