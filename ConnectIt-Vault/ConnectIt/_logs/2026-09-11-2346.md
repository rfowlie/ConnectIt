---
source: process-discussion
run: 2026-09-11-2346
scope: ConnectIt
tags:
  - log
  - discussion
---

## Request

User invoked `/process-discussion` with a pasted transcript (the "Actions setup 1.m4a"
recording) — a solo design discussion on the SWAP action and its UI.

## Overview

Ran the transcript through `/process-discussion` per
[`_core/_schema/_discussions.md`](../../_core/_schema/_discussions.md): identified five
topics (swap action UI, the GameMode-side actions mediator, the HUD/PlayerController
interface pattern, the two SWAP design variants, and the Smash-Bros-inspired
actions-as-items vision), extracted Tasks/Deliverables/Questions/Workflows from the
content, and wrote the note with the full transcript preserved verbatim. This is the
**first note in `_discussions/`** anywhere in the vault, so every topic resolved as "first
time this came up" — no prior notes to link back to.

## Created

- [`ConnectIt/_discussions/2026-09-11-swap-action-ui-design.md`](../_discussions/2026-09-11-swap-action-ui-design.md)

## Changed

None.

## Flags & Follow-ups

- Several naming/design questions in the transcript were left genuinely open rather than
  resolved (see the note's `## Questions`): tags vs. `TSubclassOf<TurnBasedAction>` for
  available actions; the mediator's eventual name (Board Request / Game State / Actions
  Mediator); and which of the two SWAP-action variants (bonus vs. alternate-mandatory) to
  build — the note records these as unresolved, doesn't pick one.
- No owners are attached to the `## Tasks` checkboxes — this was a solo design session
  with no assignment discussed.
- `ConnectIt/_discussions/__INDEX.md` was **not** created — `/process-discussion` only
  updates an index if one already exists, and none did. Worth adding one by hand if this
  section keeps growing.
