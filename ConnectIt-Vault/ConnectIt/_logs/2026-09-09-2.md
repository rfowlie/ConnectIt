---
source: claude-code
run: 2026-09-09
scope: ConnectIt
tags: [log, cleanup, libraries]
---

## Request

Move all blueprint function libraries from `Source/ConnectIt/(Public|Private)/Library/` to `Source/ConnectIt/(Public|Private)/Framework/Library/`, and fix up any includes.

## Overview

Moved 5 header/`.cpp` pairs via `git mv` (preserving history): `ConnectIt_BoardStateLibrary`, `ConnectIt_GameRulesLibrary`, `ConnectIt_GameUtilityLibrary`, `ConnectIt_GridFloodLibrary`, `ConnectIt_GridRulesLibrary`. Removed the now-empty old `Library/` directories, and fixed `#include "Library/ConnectIt_*.h"` → `#include "Framework/Library/ConnectIt_*.h"` across all 17 dependent `.cpp` files (`Action/`, `Framework/Controller/`, `Framework/Data/`, `Framework/GameMode/`, `Framework/GameState/`, `Framework/Library/` self-includes, `Framework/Subsystem/`, `MinMax/`, `UI/`). Confirmed no stray old-path references remained (the one other `Library/` hit found, `CodingUtilsLibrary.h`, belongs to a different plugin and was correctly left alone).

## Created

None — pure relocation.

## Changed

- 5 library header/`.cpp` pairs relocated to `Framework/Library/`
- 17 dependent files' `#include` paths updated

## Flags & Follow-ups

None.
