---
source: session
run: 2026-09-14-0044
scope: ConnectIt
tags:
  - log
---

## Request

Open a `ConnectIt/_questions/` folder and log the first open question: what to do about
action gameplay tags currently needing to be set in 4-5 separate, disconnected places for
the system to function.

## Overview

`_questions` is an existing vault-wide section type, already instantiated at
`_core/_questions/` and `Development/optimal-co-developer/_questions/` — this opens a
third instance at `ConnectIt/_questions/`. Wrote the question note grounding "4-5 places"
in the actual known instances from this session's work (ini tag declaration, per-instance
`ActionTag`, native `RequestType` tag, `RequiredActionTagA`/`B` on the new actions
component) and cross-linked it to the already-tracked `ActionTag`/`RequestType` fix in
`_tasks/active.md`, which is one symptom of this broader question, not the whole answer.

## Created

- [`ConnectIt/_questions/__README.md`](../_questions/__README.md)
- [`ConnectIt/_questions/action-gameplay-tags.md`](../_questions/action-gameplay-tags.md)
  — `status: open`, not answered here.

## Changed

- [`ConnectIt/CLAUDE.md`](../CLAUDE.md) — added `_questions/` to the Sections list.
- [`ABOUT.md`](../../ABOUT.md) — added the new instance to the `questions` section-table
  row. No changelog bullet — `_questions` isn't a new section type (already registered),
  same treatment as the earlier `ConnectIt/_tasks/` instantiation.

## Flags & Follow-ups

- This is a genuinely open design question, not resolved here — no "single source of
  truth" mechanism proposed yet. Worth an `/ask-partner` or dedicated design pass before
  the next new action makes the sprawl worse.
- Related, already tracked: `ConnectIt/_tasks/active.md` — "Fix
  `ConnectIt_PlacePieceAction`'s `ActionTag`/`RequestType` conflation" is one concrete
  symptom of this question, not a substitute for answering it.
