---
source: session-close
run: 2026-09-17-1615
scope: ConnectIt
tags:
  - log
---

## Request

`/session-close` for today's (2026-09-17) session.

## Overview

Updated [`ConnectIt/_sessions/2026-09-17-1454.md`](../_sessions/2026-09-17-1454.md)'s
`## Close` in place (per the earlier backfill log's own flag, rather than splitting a new
session note, since no fresh `/session-open` ran for this later chunk) to cover the work
done after the 1455 backfill: the `UActorLerpComponent`/`BuildRaisedShiftPath` build, the
HUD wiring added then removed, the board-shift-loop-visuals question, and the piece-actor
exploration. Ran the decisions/questions/discussions routing step required by
`/session-close`.

## Created

- [`ConnectIt/_decisions/2026-09-17-visual-events-live-in-blueprint-not-cpp.md`](../_decisions/2026-09-17-visual-events-live-in-blueprint-not-cpp.md)
  — visual reactions to gameplay events live in Blueprint, not C++.

## Changed

- [`ConnectIt/_sessions/2026-09-17-1454.md`](../_sessions/2026-09-17-1454.md) — `## Close`
  updated in place with everything since the 1455 backfill.
- [`ConnectIt/_decisions/__INDEX.md`](../_decisions/__INDEX.md) — new row added.

## Flags & Follow-ups

- **Question candidate, not filed:** piece-actor tile-tracking during a board shift (the
  `ActivatePieceAt`/`GetPieceAtPosition`/`GetPositionOfPiece` gaps found this session, plus
  the open "C++ method vs Blueprint-only" question for re-keying the piece registry's
  position map). Genuinely open and easy to forget — owner's call whether it gets a
  `_questions/` note.
- **No `_discussions/` filing offered** — the earlier 5-question technical exchange
  (`Is Local Controller`, the `GetPlayerState` faction-index bug, `TryPushAction`,
  `SetSwapActionUsesRemaining`'s dead `HasAuthority()` check, `TryConsumeSwapUse` naming)
  was already filed via `/process-discussion` earlier this session, not re-offered. The
  later loop-visuals/piece-tracking exchange is judged already captured by the
  `board-shift-loop-visuals` question note and this close's re-entry point — not
  substantial enough on its own to warrant a separate verbatim `_discussions/` note.
