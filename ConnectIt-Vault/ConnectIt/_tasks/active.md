# ConnectIt tasks — active

| Task                                                                       | Created    | Target | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------------------------------------------- | ---------- | ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Brainstorm other required board rules beyond `IsTilePlaceable`             | 2026-09-12 | —      | active | From [2026-09-12 — Board Request Mediator & Player Action Config Design](../_discussions/2026-09-12-board-mediator-and-action-config-design.md) — `IsTilePlaceable` itself shipped same day, this brainstorm didn't.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Confirm Actions UI's HUD-owned turn-start wiring is complete                | 2026-09-14 | —      | active | Was "Create Root action class which shows the actions UI" — superseded. A root/do-nothing action was going to display the UI, but the UI would disappear the moment the stack switched to the selected action, which isn't wanted. Settled instead on the HUD owning the UI: hooked to `OnTurnStart`, shown/hidden by local-player-turn check, persists across action selection (selected button highlights + disables). See [2026-09-14 — Actions UI owned by HUD](../_decisions/2026-09-14-actions-ui-owned-by-hud.md). Remaining work, if any, is just confirming this wiring is fully in place — not building a root action. |
| Stop hardcoding "Place Piece"/"Swap Piece" buttons in the Actions UI       | 2026-09-14 | —      | active | From the level-config-fallback debugging session: the Actions UI was rapid-prototyped to always show both buttons regardless of what's actually available, instead of reading the player's real available actions (loadout + `SwapUsesRemaining`, etc.) from `PlayerState`. Now that the underlying data is confirmed working end to end (SWAP uses, level config fallback), worth reading real availability instead of hardcoding the two buttons.                                                                                                                                                                                                                                    |
| Design and implement the Board Shift action against the new action-state shape | 2026-09-14 | —      | active | Board Shift is the deliberate second data point for the action-state architecture: `PlayerState` holds pure action-state data, `ActionsComponent` is the builder (queries `PlayerState`, configures, runs), and writes only ever happen through the existing request → server-verify → Mediator → replication pipeline — never `ActionsComponent` writing `PlayerState` directly. See [2026-09-14 — Action state on PlayerState, ActionsComponent as builder](../_decisions/2026-09-14-action-state-on-playerstate-actionscomponent-as-builder.md) and [2026-09-14 check-in](../../Development/optimal-co-developer/_meetings/2026-09-14-check-in.md). |
| Generalize `CanAutoEndTurn`'s turn-end requirement logic to configurable AND/OR groups | 2026-09-14 | —      | active | Current `RequiredActionTagA`/`RequiredActionTagB` is a fixed two-tag OR pair (SWAP's alternate-turn-end case). A planned adventure-mode level needs an AND case (forced board-shift *and* place-piece, same turn), which the current shape can't express. Needs to become an array of requirement groups, each with its own any/all mode — actual shape deferred until Board Shift's real requirements are known. See [2026-09-14 — Turn-end requirements need AND/OR groups](../_decisions/2026-09-14-turn-end-requirements-need-and-or-groups.md). |
| Prototype blocker grid pieces (non-faction, health, damage-on-adjacent-score) | 2026-09-14 | —      | active | Sequenced after Board Shift ships, not alongside it. Non-faction-owned grid pieces with health; take damage when a player scores on an adjacent tile; break after enough damage, freeing the tile for placement (via `IConnectIt_TilePlaceableRule`) and granting the last-hit player a one-time-use action. Open design question, not yet settled: how a non-faction piece should be represented in `FConnectItBoardState` — a `BlockerPositions`/`BlockerHealth` parallel-array pair (mirroring the existing `TilePositions`/`TileDataArray` split) was floated but not confirmed. See [2026-09-14 check-in](../../Development/optimal-co-developer/_meetings/2026-09-14-check-in.md). |
| Confirm `DefaultLevelConfig` is set in Project Settings                    | 2026-09-14 | —      | active | The level-config-fallback fix only does anything useful once this is actually set (ConnectIt Level Config settings). Flagged as the one outstanding manual step in [2026-09-14 — Level config default fallback](../_decisions/2026-09-14-level-config-default-fallback.md); owner reported both levels working during the 2026-09-14 check-in but this specific setting wasn't explicitly confirmed. |

<!--
Status: active | suspended | complete | dropped
Change a Status cell, then run /process-tasks to file the row.
-->
