# ConnectIt tasks — active

| Task                                                                             | Created    | Target | Status | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------------------------------------------------- | ---------- | ------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Brainstorm other required board rules beyond `IsTilePlaceable`                   | 2026-09-12 | —      | active | From [2026-09-12 — Board Request Mediator & Player Action Config Design](../_discussions/2026-09-12-board-mediator-and-action-config-design.md) — `IsTilePlaceable` itself shipped same day, this brainstorm didn't.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Fix `ConnectIt_PlacePieceAction`'s `ActionTag`/`RequestType` conflation          | 2026-09-12 | —      | active | From the SWAP action plan's Gotcha 1: `HandleValidSelection_Implementation` sets `Request.RequestType = ActionTag;` instead of the native `ConnectIt_Game_PlacePiece` tag — only dispatches today because someone hand-set the editor `ActionTag` value to the native tag string (`"ConnectIt.Game.State.PlacePiece"`), silently diverging from the ini-declared `"ConnectIt.Action.PlacePiece"`. In the editor: confirm PlacePiece's `ActionTag` is still set that way. Real fix: hardcode `Request.RequestType = ConnectIt_Game_PlacePiece;` in code (mirroring how `ConnectIt_PieceSwapperAction` was written) so `ActionTag` is free to hold the clean ini-declared value instead.                                                                                                       |
| Verify player loadout's `RootActionClass` is a do-nothing action, not PlacePiece | 2026-09-12 | —      | active | From the SWAP action session: `UConnectIt_TurnBasedActionsComponent::CanAutoEndTurn_Implementation` (the new PlacePiece/SWAP OR'd turn-end override) only sees actions via `GetRequiredActions()`, which scans `RuntimeActions` (the loadout's `Actions[]` array) — **not** `RootAction`, which is vended separately from `RootActionClass` and never added to `RuntimeActions`. If `RootActionClass` on the player loadout is actually `ConnectIt_PlacePieceAction` rather than a separate do-nothing action, PlacePiece is invisible to the new override and the turn-end logic silently breaks. Vault docs (`UActionLoadoutDataAsset.md`, `add-a-turn-action.md`) say the intended pattern is a do-nothing root, but this couldn't be verified from source alone — confirm in the editor. |
| Re-check `ActionsComponent` Class Defaults after its concrete class change       | 2026-09-12 | —      | active | From the SWAP action session: `AConnectIt_PlayerController`'s new constructor swaps `ActionsComponent`'s concrete class from `UTurnBasedActionsComponent` to the new `UConnectIt_TurnBasedActionsComponent` via `FObjectInitializer::SetDefaultSubobjectClass`. If a Blueprint child of `AConnectIt_PlayerController` overrides any `ActionsComponent` properties via Class Defaults (e.g. `bAutoEndTurnOnAllRequiredActionsCompleted`), confirm those overrides still show and apply correctly post-compile — property overrides are keyed by subobject name ("ActionsComponent") and should carry over, but this wasn't empirically verified.                                                                                                                                              |
| Create Root action class which shows the actions UI                              | 2026-09-12 |        | active | When a players turn starts they will always see the UI set. We can test this for now then later work on the idea of auto setting place piece as the active action on turn start.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

<!--
Status: active | suspended | complete | dropped
Change a Status cell, then run /process-tasks to file the row.
-->
