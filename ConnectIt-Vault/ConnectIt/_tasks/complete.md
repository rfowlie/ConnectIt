# ConnectIt tasks — complete

| Task | Created | Target | Status | Notes | Moved |
|---|---|---|---|---|---|
| Fix `MustImplement` UPROPERTY setup on the pluggable board rules (`Instanced` + `TObjectPtr<UObject>`, not `TScriptInterface`) | 2026-09-12 | — | complete | From [2026-09-12 — Board Request Mediator & Player Action Config Design](../_discussions/2026-09-12-board-mediator-and-action-config-design.md). Shipped same day, before this tracker existed. | 2026-09-12 |
| Add `IsTilePlaceable` as a customizable BoardRule (was hardcoded to "no other piece is there") | 2026-09-12 | — | complete | Same discussion. Shipped same day, before this tracker existed. | 2026-09-12 |
| Verify player loadout's `RootActionClass` is a do-nothing action, not PlacePiece | 2026-09-12 | — | complete | From the SWAP action session: `UConnectIt_TurnBasedActionsComponent::CanAutoEndTurn_Implementation` (the new PlacePiece/SWAP OR'd turn-end override) only sees actions via `GetRequiredActions()`, which scans `RuntimeActions` (the loadout's `Actions[]` array) — **not** `RootAction`, which is vended separately from `RootActionClass` and never added to `RuntimeActions`. If `RootActionClass` on the player loadout is actually `ConnectIt_PlacePieceAction` rather than a separate do-nothing action, PlacePiece is invisible to the new override and the turn-end logic silently breaks. Vault docs (`UActionLoadoutDataAsset.md`, `add-a-turn-action.md`) say the intended pattern is a do-nothing root, but this couldn't be verified from source alone — confirm in the editor. | 2026-09-14 |
| Fix `ConnectIt_PlacePieceAction`'s `ActionTag`/`RequestType` conflation | 2026-09-12 | — | complete | From the SWAP action plan's Gotcha 1: `HandleValidSelection_Implementation` sets `Request.RequestType = ActionTag;` instead of the native `ConnectIt_Game_PlacePiece` tag — only dispatches today because someone hand-set the editor `ActionTag` value to the native tag string (`"ConnectIt.Game.State.PlacePiece"`), silently diverging from the ini-declared `"ConnectIt.Action.PlacePiece"`. Fixed by hardcoding `Request.RequestType = ConnectIt_Game_PlacePiece;` in code (mirroring `ConnectIt_PieceSwapperAction`), freeing `ActionTag` to hold the clean ini-declared value. Owner confirmed complete during the 2026-09-14 check-in. | 2026-09-14 |
| Re-check `ActionsComponent` Class Defaults after its concrete class change | 2026-09-12 | — | complete | From the SWAP action session: `AConnectIt_PlayerController`'s new constructor swaps `ActionsComponent`'s concrete class from `UTurnBasedActionsComponent` to the new `UConnectIt_TurnBasedActionsComponent` via `FObjectInitializer::SetDefaultSubobjectClass`. Any Blueprint-child Class Defaults overrides on `ActionsComponent` (e.g. `bAutoEndTurnOnAllRequiredActionsCompleted`) were confirmed to still carry over post-compile. Owner confirmed complete during the 2026-09-14 check-in. | 2026-09-14 |
