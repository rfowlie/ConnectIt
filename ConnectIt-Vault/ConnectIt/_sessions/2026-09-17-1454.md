---
date: 2026-09-17
tags:
  - session
---

# session — 2026-09-17-1454

_(backfilled — `/session-open` was never actually called this morning; this note is
reconstructed after the fact from today's `_logs/` entries and real file-modification
timestamps rather than written live at the start of the day.)_

## Open — the map

**Where we are:**
- `GetTilesByDirection` fixed yesterday; Board Shift's remaining pieces (action-side
  selection, request struct, Mediator handler) hadn't been started yet as of this
  morning.
- The `action-gameplay-tags` open question got a real root-cause finding: the `ActionTag`
  Instanced-sub-object divergence, candidate fixes recorded but not applied.
- Owner had independently started hand-editing `ActionTag` (moved off `UTurnBasedActionBase`
  entirely, replaced with a `GetActionTag()` accessor) and the input-binding system
  (`FInputTagBinding` gained a per-binding delegate) — both mid-flight, untested against a
  full rebuild.

**Playable:** Same as yesterday's close — nothing new reached PIE yet at the start of
today.

**Off the table:** Nothing formally ruled out yet at day-start; carried forward from
yesterday's re-entry point, not a new decision.

**Next thing:** Finish Board Shift end to end — `HandleValidSelection_Implementation`,
the request/payload shape, and the Mediator's `HandleBoardShiftRequest`.

**Not touching:** Whatever the AND/OR turn-end generalization and blocker pieces still
are — unchanged, still explicitly deferred per the 2026-09-14 decisions.

## Close — the reckoning

_(written same-day, from the actual work done)_

**What changed:**
- `action-gameplay-tags` question note updated with the `ActionTag`-instancing root cause
  and two candidate fixes (`EditDefaultsOnly`, or a fully code-derived accessor) — owner
  chose to hold off applying either for now.
- Reviewed and fixed the owner's own `ActionTag`/input-binding refactor: 13 sites across 3
  files that would not have compiled (`ActionTag` read as a bare member after the field
  was removed), the entire retired tag-broadcast input chain removed as dead code
  (`OnBoundInputTriggered`, `HandleInputTagTriggered`, `OnInputTagTriggered`,
  `BindingTag`), `ConnectIt_SwapPieceAction`'s tags brought to `EditDefaultsOnly` to match
  `PlacePieceAction`. Two related vault code pages updated to match.
- `UConnectIt_BoardShiftAction::HandleValidSelection_Implementation` implemented — builds
  and sends the shift request; hover-cleared/selection-state cleanup added so the
  previewed line reverts correctly.
- `FConnectItRequestBoardShift` and `FConnectItBoardChangeEvent` extended with the new
  Board Shift fields; `ConnectIt_Game_Shift` / `ConnectIt_Event_BoardShifted` tags added.
- `UConnectIt_BoardRequestMediator::HandleBoardShiftRequest` implemented from scratch —
  not via `UGridMechanics_GridShiftLibrary` (abandoned — it only models orthogonal
  Row/Column shifts, no diagonal support) but a direct rotation over the
  already-ordered `Positions` array, wrapping the far end to the near end, moving whole
  tile data (not just the piece). Re-validates every position server-side rather than
  trusting the client's array.
- Added `FConnectItTileData::bCanShift` — an unshiftable tile is skipped entirely during
  a shift; the shiftable tiles around it rotate as if it weren't in the line.
- Started `ConnectIt/design/` (deliberately schema-less) — notes for Place Piece, SWAP,
  and Board Shift's actual behavior + gameplay ramifications, plus a general "tiles with
  powers" feature note (5 example powers, including a poison-tile sketch) tied to the
  real scoring mechanic (`ApplyScoringLine` already increments every tile in a completed
  line's multiplier — a free "charge" counter a power system can key off of).

**Playable now:** Board Shift is now functionally complete server-side — select a tile,
the line shifts, scoring re-runs, the event queues for visual sequencing. Not yet
playtested in PIE this session; that's the natural next check, not a claim it's confirmed
working end to end in-editor.

**Ruled out:** Using `UGridMechanics_GridShiftLibrary`/`FShiftOperation` for the Mediator's
shift computation — it can't express diagonal shifts, and `ConnectIt_BoardShiftAction`
already supports all 8 `EGridDirection` values via `GetTilesByDirection`. Rewrote the
computation directly instead, working uniformly for every direction.

**Re-entry point:** PIE-test Board Shift for real — hover preview, selection, the actual
shift, wrap-around, and a `bCanShift = false` tile behaving as an anchor. Separately, the
`action-gameplay-tags` `EditDefaultsOnly`/code-derived-accessor decision is still open —
owner is still mulling it over, not a re-entry blocker.

**Deadline check:** Not discussed this session.
