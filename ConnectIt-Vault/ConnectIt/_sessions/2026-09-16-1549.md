---
date: 2026-09-16
tags:
  - session
---

# session — 2026-09-16-1549

## Open — the map

**Where we are:** SWAP shipped end-to-end (PlayerState-authoritative use budget,
alternate-mandatory turn-end, faction-ownership + re-scoring on the Mediator) and the
level-config-fallback bug is fixed on both maps. The Actions UI question and the
action-state architecture question both resolved in the
[2026-09-14 check-in](../../Development/optimal-co-developer/_meetings/2026-09-14-check-in.md)
— HUD owns the UI, `PlayerState` holds pure action-state data with `ActionsComponent` as
the builder, writes only through the Mediator pipeline. Board Shift is the committed next
action, specifically as the second data point for that architecture.

**Playable:** A stranger could play a full basic match on either level: place pieces, swap
pieces (budget-limited, server-enforced), turns alternating correctly, scoring re-running
on each mutation. Nothing beyond that — no Board Shift, no blockers, no AI opponent
mentioned as playable.

**Off the table:**
- Root-action-on-the-stack showing the Actions UI — killed because the UI would disappear
  the instant the stack switched to the selected action; HUD-owned/turn-gated instead. See
  [`_decisions/2026-09-14-actions-ui-owned-by-hud.md`](../_decisions/2026-09-14-actions-ui-owned-by-hud.md).
- The action-state "builder" living on `PlayerState` — killed over replication risk (real
  work needs server authority, `PlayerState` is supposed to be passive replicated data);
  `ActionsComponent` is the builder instead. See
  [`_decisions/2026-09-14-action-state-on-playerstate-actionscomponent-as-builder.md`](../_decisions/2026-09-14-action-state-on-playerstate-actionscomponent-as-builder.md).

**Next thing:** `ConnectIt/CLAUDE.md` no longer states an explicit single next action (its
"Current state" section was deliberately removed on 2026-09-14 as stale) — that gap is
real. From the check-in's `## Next` and the current
[`_tasks/active.md`](../_tasks/active.md), the clear candidate is: **design and implement
the Board Shift action** against the new `PlayerState`-data / `ActionsComponent`-builder
shape. Two smaller candidates sit ahead of or alongside it: confirming `DefaultLevelConfig`
is actually set in Project Settings, and confirming the Actions UI's HUD/turn-start wiring
is fully in place.

**Not touching:** Blocker grid pieces — explicitly sequenced after Board Shift ships, not
alongside it. The turn-end AND/OR requirement-group generalization — direction agreed,
shape deliberately deferred until Board Shift's real requirements are known. De-hardcoding
the Actions UI's Place/Swap buttons — open, not urgent, no dependency on Board Shift.

## Close — the reckoning

_(backfilled 2026-09-17 — `/session-close` was never actually called this day, so this is
reconstructed from the day's `_logs/` entries and real file-modification timestamps rather
than written live. Flagged so it's not mistaken for a normal same-day reckoning.)_

**What changed:**
- Repo-root `.claude/skills/` mirrors (6 stale skills, diverged from this vault's authored
  copies) removed.
- `ConnectIt/CLAUDE.md` gained a permanent guardrail against ever re-adding a "Current
  state" section; `session-open`/`session-close` rewritten to source state from
  `_tasks/active.md` + `_meetings/` + `_decisions/` instead.
- `/session-open`'s map output switched to bullet-point-per-block, per owner preference
  (saved to cross-session memory too).
- Vault tooling: `process-data` → `process-atomic` ordering fixed as a stated dependency, a
  `TSchemaWiki` template added, `process-atomic` extended with a second pass that sweeps
  `_data/wiki/` notes into `_core/_atomic/`, atomic-note word cap raised 500 → 1000. (This
  one — [`_core/_logs/2026-09-16-1610.md`](../../_core/_logs/2026-09-16-1610.md) — has no
  matching turn in this conversation's own record; it ran in a session this backfill
  wasn't part of. Included here on the log's own authority, not from direct memory.)
- `UGridTileRegistryBase::GetTilesByDirection` rewritten — the prior version didn't
  compile (`IsValid(Tile)()`) and had no `return` statement; the fix walks the correct
  row/column/diagonal line through a position in one pass, tolerating gaps.

**Playable now:** Unchanged from the day's start — nothing above reached a playable
end-to-end state yet. `GetTilesByDirection` is a real, necessary fix but purely internal;
`BoardShiftAction` didn't yet call it in a way that reached PIE this day.

**Ruled out:** Nothing design-level ruled out today — this was process/tooling work plus
one internal bug fix, not a design decision day.

**Re-entry point:** Board Shift — `GetTilesByDirection` now returns the correct line, so
the next concrete step is finishing `UConnectIt_BoardShiftAction::HandleValidSelection_Implementation`
and the Mediator's request handling. (This is exactly what the next day's work — see
[2026-09-17-1454](2026-09-17-1454.md) — turned out to be.)

**Deadline check:** Not discussed this day.
